// --- 設定檔 ---

// 請將 'localhost' 改成您電腦在區域網路中的 IP 位址
// (Windows: 在 cmd 中執行 ipconfig; Mac/Linux: 在終端機中執行 ifconfig)
// 範例: const SERVER_IP = '192.168.1.100';
const SERVER_IP = '10.215.199.143'; //'192.168.2.102';//'192.168.0.160';

// 伺服器將會運行的埠號 (port)
// 如果這個埠號被佔用，您可以改成其他數字 (例如 5002, 8000, 8080)
const SERVER_PORT = 5001;

// --- 遊戲手感調整 ---

// 時間偏移量 (單位: 毫秒)
// 用於校準玩家的視覺反應與實際動作之間的延遲。
// 如果您感覺總是太晚或太早，可以調整此數值。
// 正數表示您預期玩家會比節奏點"晚"一點做出反應。
const TIME_OFFSET = -2100;

// 觸發時機範圍 (單位: 毫秒)
// 判定為 "成功" 的時間誤差範圍。數值越大，判定越寬鬆。
// 例如：500 表示在校準後的節奏點前後 500ms 內的動作都算成功。
const TIMING_RANGE = 250; 